services:
  - type: web
    name: tushar-sakshi-engagement
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
